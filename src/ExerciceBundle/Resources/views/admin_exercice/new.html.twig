{% extends 'layout_admin.html.twig' %}

{% block content %}
    <h1>Nouvel exercice</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter cet exercice" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_exercice_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
