{% extends "layout_user.html.twig" %}

{% block lancer_actif %} class="active" {% endblock %}

{% block content %}





<div class="row">
	<div class="jumbotron story padding-10 padding-lr-30">
	{% if not story.sound is empty %}
			<!-- <p><audio src="{{asset(story.getSoundView())}}" controls>Veuillez mettre à jour votre navigateur !</audio></p> -->
			<div class="monaudio">
				<a href="javascript:void(0)" onclick="aud_play_pause(this)">
				    <i class="control glyphicon glyphicon-play"></i>
				    <audio class="xnine-player" src="{{asset(story.getSoundView())}}" preload="auto"></audio>
				</a>
			</div>

		{% endif %}

		<h1 >{{story.title | capitalize }}</h1><br>
		<p >{{story.text | nl2br }}</p>

		
	</div>
</div>


<form action="{{ path('quiquandou_check') }}" method="POST">

	{% include 'ExerciseBundle:Default:question.html.twig' 
		with {icon: 'glyphicon glyphicon-user', question: 'qui', form: 'qui', thumbnails_liste: thumbnails_qui} %}
	{% include 'ExerciseBundle:Default:question.html.twig' 
		with {icon : 'glyphicon glyphicon-hourglass', question: 'quand', form: 'quand', thumbnails_liste: thumbnails_quand} %}
	{% include 'ExerciseBundle:Default:question.html.twig' 
		with {icon : 'glyphicon glyphicon-screenshot', question: 'où', form: 'ou', thumbnails_liste: thumbnails_ou} %}
<br clear="all">
<br>
	<div class="row" style="margin-top:15px;">
		<div class="col-sm-6 col-sm-offset-3">
			<button type="submit" class="btn btn-primary  btn-lg btn-block"> Valider mes réponses</button>
		</div>
		<!--<div class="col-sm-2">
			<button type="submit" class="btn btn-danger  btn-lg btn-block">J'ai terminé</button>
		</div>-->
	</div>

	
</form>

<br>

{% endblock %}


{% block javascripts %}

<script>

var quest = ["qui","quand","ou"];

function toggleQuestion(elt){
	
	$( ".question" ).each(function( index ) {

		var select = '#'+$(this).attr('id') +' .answers'; 
		//alert(  $(elt).attr('id') +'!='+ $(this).attr('id')   );
		if( $(elt).attr('id') != $(this).attr('id') ){			
			$(  select ).hide();
		}
		else{			
			$( select ).show(500);
		}
	});

	/*for(var 0= 1; i < 3; i++){
		if(quest[i] != id){
			$('#q_'+quest[i]).slideUp();
		}
		else{
			$('#q_'+quest[i]).slideDown();
		}
	}*/

}

$( document ).ready(function() {
	
	$('.question .title').click(function(){		
		toggleQuestion( $(this).parent() );
	});
    
	toggleQuestion( $('.question:first') );
});



</script>

{% endblock %}