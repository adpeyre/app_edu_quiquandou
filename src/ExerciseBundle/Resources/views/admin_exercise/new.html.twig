{% extends 'layout_admin.html.twig' %}

{% block content %}
    <h1>Nouvel exercise</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter cet exercise" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_exercise_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
