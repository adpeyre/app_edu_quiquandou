{% extends 'layout_admin.html.twig' %}

{% block content %}
    <h1>Classrooms list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Year</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for classroom in classrooms %}
            <tr>
                <td><a href="{{ path('admin_classroom_show', { 'id': classroom.id }) }}">{{ classroom.id }}</a></td>
                <td>{{ classroom.name }}</td>
                <td>{{ classroom.year }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_classroom_show', { 'id': classroom.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_classroom_edit', { 'id': classroom.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_classroom_new') }}">Create a new classroom</a>
        </li>
    </ul>
{% endblock %}
